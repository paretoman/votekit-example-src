{
  "devDependencies": {
    "del-cli": "^4.0.1",
    "parcel": "^2.2.1"
  },
  "scripts": {
    "build": "npm run clean && parcel build _site/*.html --public-url ./ --dist-dir dist && parcel build _site/about/*.html --public-url ./ --dist-dir dist/about",
    "clean": "del-cli \"dist/**\" \"!dist/.git\""
  }
}
